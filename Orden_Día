<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edyen · Orden del Día — Consejo de Dirección</title>
  <meta name="theme-color" content="#0b66ff" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root{
      /* Colores inspirados en el logotipo */
      --brand:#0b66ff; /* azul edyen */
      --green:#36e000; /* acento verde */
      --red:#ff1a00;   /* acento rojo */
      --gold:#f7b24a;  /* acento amarillo */
      --ink:#0b1220;   /* texto */
      --muted:#5b6b8c; /* texto secundario */
      --bg:#f7f9ff;    /* fondo suave */
      --card:#ffffff;  /* tarjetas */
      --line:#e6edff;  /* bordes sutiles */
      --radius:18px;
      --shadow: 0 10px 28px rgba(16,24,40,.06), 0 2px 8px rgba(16,24,40,.05);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:"Inter", ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial;
      font-weight:300; /* tipografía light */
      background:linear-gradient(180deg,var(--bg),#fff);
      color:var(--ink);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }
    .wrap{
      max-width:980px;
      margin-inline:auto;
      padding:48px 24px 64px;
    }
    header{
      display:grid;
      gap:6px;
      margin-bottom:28px;
    }
    .eyebrow{
      font-size:.9rem;
      letter-spacing:.12em;
      text-transform:uppercase;
      color:var(--muted);
    }
    h1{
      margin:0;
      font-weight:600;
      letter-spacing:-.02em;
      font-size: clamp(28px, 4vw, 40px);
    }
    .meta{
      color:var(--muted);
      font-size:1rem;
    }
    .list{
      display:grid;
      gap:16px;
      margin-top:26px;
    }
    .card{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:18px;
      padding:18px 18px 18px 20px;
      background:var(--card);
      border:1px solid var(--line);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
    }
    .card:hover{ transform:translateY(-1px); box-shadow:0 14px 34px rgba(16,24,40,.08), 0 3px 10px rgba(16,24,40,.06);}
    .card .who{
      display:flex; flex-direction:column; gap:4px; min-width:0;
    }
    .name{
      font-weight:400;
      font-size:1.05rem;
    }
    .topic{
      color:var(--muted);
      font-size:.98rem;
      overflow:hidden;
      text-overflow:ellipsis;
      white-space:nowrap;
    }
    .cta{
      display:inline-flex; align-items:center; justify-content:center;
      padding:10px 16px;
      border-radius:12px;
      border:1px solid transparent;
      background:var(--brand);
      color:#fff;
      font-weight:500;
      text-decoration:none;
      transition:filter .2s ease, transform .05s ease;
      white-space:nowrap;
    }
    .cta:hover{ filter:brightness(1.05) saturate(1.05); }
    .cta:active{ transform:translateY(1px); }

    /* Acentos sutiles por ítem (barra izquierda) */
    .card::before{
      content:"";
      width:6px; height:100%;
      border-radius:10px 0 0 10px;
      margin-right:8px;
      background:var(--brand);
      display:block;
    }
    .card.green::before{ background:var(--green); }
    .card.red::before{ background:var(--red); }
    .card.gold::before{ background:var(--gold); }

    /* Accesibilidad para foco teclado */
    .card:focus-within{ outline:2px solid rgba(11,102,255,.35); outline-offset:3px; }

    footer{
      margin-top:36px;
      color:var(--muted);
      font-size:.95rem;
      text-align:center;
    }

    /* Layout pequeño */
    @media (max-width:640px){
      .card{ flex-direction:column; align-items:flex-start; }
      .cta{ width:100%; }
    }
  </style>
</head>
<body>
  <main class="wrap">
    <header>
      <div class="eyebrow">Orden del Día</div>
      <h1>Sesión del Consejo de Dirección</h1>
      <p class="meta">Los siguientes miembros del Consejo han compartido material para presentar en la sesión del <strong>02 de septiembre de 2025</strong>.</p>
    </header>

    <section class="list" aria-label="Material de la sesión">
      <div class="card">
        <div class="who">
          <div class="name">Eric Barrera</div>
          <div class="topic">AVANCE ISO 9001</div>
        </div>
        <a class="cta" href="https://edyen-chief-staff.github.io/Consejo/" target="_blank" rel="noopener">Abrir</a>
      </div>

      <div class="card green">
        <div class="who">
          <div class="name">Lucero José</div>
          <div class="topic">REPORTE SEMANAL DE VACANTES</div>
        </div>
        <a class="cta" href="https://gamma.app/docs/REPORTE-SEMANAL-DE-VACANTES-s943r8ssyhkzm36?mode=doc" target="_blank" rel="noopener">Abrir</a>
      </div>

      <div class="card red">
        <div class="who">
          <div class="name">Federico Curiel</div>
          <div class="topic">CONTRATO INVERSIONISTAS</div>
        </div>
        <a class="cta" href="https://docs.google.com/presentation/d/1T1-ENrzq1pna9dIVW32rYBO8CaFKJlbi/edit?usp=drive_link&ouid=113873578964714878242&rtpof=true&sd=true" target="_blank" rel="noopener">Abrir</a>
      </div>

      <div class="card gold">
        <div class="who">
          <div class="name">Alejandra Luján</div>
          <div class="topic">INICIO DE OPERACIONES</div>
        </div>
        <a class="cta" href="https://docs.google.com/presentation/d/1_YKkHDaxUhscwQrAzMUcHPusoHOGS6or/edit?usp=drive_link&ouid=113873578964714878242&rtpof=true&sd=true" target="_blank" rel="noopener">Abrir</a>
      </div>

      <div class="card red">
        <div class="who">
          <div class="name">Víctor Romo</div>
          <div class="topic">REGISTROS DE INCIDENTES OPERATIVOS</div>
        </div>
        <a class="cta" href="https://docs.google.com/presentation/d/1MWi5fyC8-EDP3tEYVREMRhk_AKa0Sk-S/edit?usp=drive_link&ouid=113873578964714878242&rtpof=true&sd=true" target="_blank" rel="noopener">Abrir</a>
      </div>
    </section>

    <footer>
      © 2025 Edyen — Página interna para la sesión del Consejo de Dirección.
    </footer>
  </main>
</body>
</html>
